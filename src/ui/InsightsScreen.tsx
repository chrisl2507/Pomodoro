/* Stage B fills this in: metric cards, sparkline, heatmap, sentences. */
export function InsightsScreen(_props: { refreshKey: number }) {
  return (
    <main className="focus-screen">
      <div className="session-label">insights land in stage B</div>
    </main>
  )
}
